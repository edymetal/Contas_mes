# Instruções do projeto

- Ao finalizar qualquer tarefa que altere o projeto, faça commit e push.
- Sempre que fizer qualquer alteração no sistema, incremente também a versão em `package.json` e `package-lock.json` na mesma entrega.
